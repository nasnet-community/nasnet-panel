# NasNetConnect Backend Configuration
# Copy this file to .env and fill in the values

# =============================================================================
# Database Configuration
# =============================================================================

# SQLite database path (relative to backend directory)
# DB_PATH=./data/system.db

# =============================================================================
# Security Configuration
# =============================================================================

# REQUIRED: Database encryption key for router credentials
# Must be a base64-encoded 32-byte (256-bit) key
# Generate with: go run -tags=dev . -generate-key
# Or: openssl rand -base64 32
#
# IMPORTANT:
# - Keep this key secure and backed up
# - Losing this key means losing access to all encrypted credentials
# - Rotating this key requires re-encrypting all credentials
#
# Example (DO NOT USE in production - generate your own):
# DB_ENCRYPTION_KEY=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=
DB_ENCRYPTION_KEY=

# JWT signing key for authentication tokens
# Must be a secure random string (at least 32 characters)
# Generate with: openssl rand -base64 32
# JWT_SECRET=

# =============================================================================
# Server Configuration
# =============================================================================

# Server listen address
# SERVER_ADDR=:8080

# Enable development mode (CORS enabled, verbose logging)
# DEV_MODE=false

# =============================================================================
# Logging Configuration
# =============================================================================

# Log level: debug, info, warn, error
# LOG_LEVEL=info

# Log format: json, console
# LOG_FORMAT=json
