// Code generated by ent, DO NOT EDIT.

package ent

import (
	"backend/ent/webhook"
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// Webhook is the model entity for the Webhook schema.
type Webhook struct {
	config `json:"-"`
	// ID of the ent.
	// ULID primary key
	ID string `json:"id,omitempty"`
	// Human-readable webhook name
	Name string `json:"name,omitempty"`
	// Webhook target URL (validated for SSRF protection)
	URL string `json:"url,omitempty"`
	// Authentication type for webhook requests
	AuthType webhook.AuthType `json:"auth_type,omitempty"`
	// Encrypted authentication credentials (AES-256-GCM encrypted JSON)
	AuthValueEncrypted []byte `json:"auth_value_encrypted,omitempty"`
	// Encryption nonce for auth_value_encrypted
	AuthNonce []byte `json:"auth_nonce,omitempty"`
	// Encrypted signing secret for request verification (AES-256-GCM)
	SigningSecretEncrypted []byte `json:"signing_secret_encrypted,omitempty"`
	// Encryption nonce for signing_secret_encrypted
	SigningNonce []byte `json:"signing_nonce,omitempty"`
	// Custom HTTP headers to include in webhook requests
	Headers map[string]string `json:"headers,omitempty"`
	// Payload template type for webhook body
	Template webhook.Template `json:"template,omitempty"`
	// Custom Handlebars template for webhook payload (required if template=custom)
	CustomTemplate string `json:"custom_template,omitempty"`
	// Whether this webhook is enabled
	Enabled bool `json:"enabled,omitempty"`
	// Total successful webhook deliveries
	SuccessCount int `json:"success_count,omitempty"`
	// Total failed webhook deliveries
	FailureCount int `json:"failure_count,omitempty"`
	// Last successful delivery timestamp
	LastSuccessAt *time.Time `json:"last_success_at,omitempty"`
	// Last failed delivery timestamp
	LastFailureAt *time.Time `json:"last_failure_at,omitempty"`
	// Record creation timestamp
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Last update timestamp
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the WebhookQuery when eager-loading is set.
	Edges        WebhookEdges `json:"edges"`
	selectValues sql.SelectValues
}

// WebhookEdges holds the relations/edges for other nodes in the graph.
type WebhookEdges struct {
	// Delivery logs for this webhook
	Logs []*NotificationLog `json:"logs,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// LogsOrErr returns the Logs value or an error if the edge
// was not loaded in eager-loading.
func (e WebhookEdges) LogsOrErr() ([]*NotificationLog, error) {
	if e.loadedTypes[0] {
		return e.Logs, nil
	}
	return nil, &NotLoadedError{edge: "logs"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Webhook) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case webhook.FieldAuthValueEncrypted, webhook.FieldAuthNonce, webhook.FieldSigningSecretEncrypted, webhook.FieldSigningNonce, webhook.FieldHeaders:
			values[i] = new([]byte)
		case webhook.FieldEnabled:
			values[i] = new(sql.NullBool)
		case webhook.FieldSuccessCount, webhook.FieldFailureCount:
			values[i] = new(sql.NullInt64)
		case webhook.FieldID, webhook.FieldName, webhook.FieldURL, webhook.FieldAuthType, webhook.FieldTemplate, webhook.FieldCustomTemplate:
			values[i] = new(sql.NullString)
		case webhook.FieldLastSuccessAt, webhook.FieldLastFailureAt, webhook.FieldCreatedAt, webhook.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Webhook fields.
func (_m *Webhook) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case webhook.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				_m.ID = value.String
			}
		case webhook.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case webhook.FieldURL:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field url", values[i])
			} else if value.Valid {
				_m.URL = value.String
			}
		case webhook.FieldAuthType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field auth_type", values[i])
			} else if value.Valid {
				_m.AuthType = webhook.AuthType(value.String)
			}
		case webhook.FieldAuthValueEncrypted:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field auth_value_encrypted", values[i])
			} else if value != nil {
				_m.AuthValueEncrypted = *value
			}
		case webhook.FieldAuthNonce:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field auth_nonce", values[i])
			} else if value != nil {
				_m.AuthNonce = *value
			}
		case webhook.FieldSigningSecretEncrypted:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field signing_secret_encrypted", values[i])
			} else if value != nil {
				_m.SigningSecretEncrypted = *value
			}
		case webhook.FieldSigningNonce:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field signing_nonce", values[i])
			} else if value != nil {
				_m.SigningNonce = *value
			}
		case webhook.FieldHeaders:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field headers", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.Headers); err != nil {
					return fmt.Errorf("unmarshal field headers: %w", err)
				}
			}
		case webhook.FieldTemplate:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field template", values[i])
			} else if value.Valid {
				_m.Template = webhook.Template(value.String)
			}
		case webhook.FieldCustomTemplate:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field custom_template", values[i])
			} else if value.Valid {
				_m.CustomTemplate = value.String
			}
		case webhook.FieldEnabled:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field enabled", values[i])
			} else if value.Valid {
				_m.Enabled = value.Bool
			}
		case webhook.FieldSuccessCount:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field success_count", values[i])
			} else if value.Valid {
				_m.SuccessCount = int(value.Int64)
			}
		case webhook.FieldFailureCount:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field failure_count", values[i])
			} else if value.Valid {
				_m.FailureCount = int(value.Int64)
			}
		case webhook.FieldLastSuccessAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_success_at", values[i])
			} else if value.Valid {
				_m.LastSuccessAt = new(time.Time)
				*_m.LastSuccessAt = value.Time
			}
		case webhook.FieldLastFailureAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_failure_at", values[i])
			} else if value.Valid {
				_m.LastFailureAt = new(time.Time)
				*_m.LastFailureAt = value.Time
			}
		case webhook.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case webhook.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Webhook.
// This includes values selected through modifiers, order, etc.
func (_m *Webhook) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryLogs queries the "logs" edge of the Webhook entity.
func (_m *Webhook) QueryLogs() *NotificationLogQuery {
	return NewWebhookClient(_m.config).QueryLogs(_m)
}

// Update returns a builder for updating this Webhook.
// Note that you need to call Webhook.Unwrap() before calling this method if this Webhook
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Webhook) Update() *WebhookUpdateOne {
	return NewWebhookClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Webhook entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Webhook) Unwrap() *Webhook {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Webhook is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Webhook) String() string {
	var builder strings.Builder
	builder.WriteString("Webhook(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("url=")
	builder.WriteString(_m.URL)
	builder.WriteString(", ")
	builder.WriteString("auth_type=")
	builder.WriteString(fmt.Sprintf("%v", _m.AuthType))
	builder.WriteString(", ")
	builder.WriteString("auth_value_encrypted=")
	builder.WriteString(fmt.Sprintf("%v", _m.AuthValueEncrypted))
	builder.WriteString(", ")
	builder.WriteString("auth_nonce=")
	builder.WriteString(fmt.Sprintf("%v", _m.AuthNonce))
	builder.WriteString(", ")
	builder.WriteString("signing_secret_encrypted=")
	builder.WriteString(fmt.Sprintf("%v", _m.SigningSecretEncrypted))
	builder.WriteString(", ")
	builder.WriteString("signing_nonce=")
	builder.WriteString(fmt.Sprintf("%v", _m.SigningNonce))
	builder.WriteString(", ")
	builder.WriteString("headers=")
	builder.WriteString(fmt.Sprintf("%v", _m.Headers))
	builder.WriteString(", ")
	builder.WriteString("template=")
	builder.WriteString(fmt.Sprintf("%v", _m.Template))
	builder.WriteString(", ")
	builder.WriteString("custom_template=")
	builder.WriteString(_m.CustomTemplate)
	builder.WriteString(", ")
	builder.WriteString("enabled=")
	builder.WriteString(fmt.Sprintf("%v", _m.Enabled))
	builder.WriteString(", ")
	builder.WriteString("success_count=")
	builder.WriteString(fmt.Sprintf("%v", _m.SuccessCount))
	builder.WriteString(", ")
	builder.WriteString("failure_count=")
	builder.WriteString(fmt.Sprintf("%v", _m.FailureCount))
	builder.WriteString(", ")
	if v := _m.LastSuccessAt; v != nil {
		builder.WriteString("last_success_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	if v := _m.LastFailureAt; v != nil {
		builder.WriteString("last_failure_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Webhooks is a parsable slice of Webhook.
type Webhooks []*Webhook
