id: '1.3-gui-install'
stage: install
title: Graphical installer engine reaches the same end state as the CLI installer
profiles: [all]
requires:
  lab: [image-tar]
steps:
  - do: install
    with: { installer: gui }
expect:
  container:
    - name: nasnet-panel
      running: true
      fields: { start-on-boot: 'yes', logging: 'yes', interface: veth-nasnet-panel }
  values:
    - path: /interface/bridge
      where: { name: containers }
    - path: /ip/address
      where: { address: 192.168.50.1/24 }
    - path: /ip/firewall/nat
      where: { comment: nasnet-panel-installer-dstnat }
      fields: { dst-port: '8080' }
  ports:
    - { from: split, port: 8080, open: true }
