import { component$ } from "@builder.io/qwik";

export const ZeroTierServerEasy = component$(() => {
  // In easy mode, ZeroTier is not shown as it's not one of the default protocols
  // This component is just a placeholder
  return null;
});