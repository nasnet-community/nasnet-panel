import { component$ } from "@builder.io/qwik";

export const SSHServerEasy = component$(() => {
  // In easy mode, SSH is not shown as it's not one of the default protocols
  // This component is just a placeholder
  return null;
});