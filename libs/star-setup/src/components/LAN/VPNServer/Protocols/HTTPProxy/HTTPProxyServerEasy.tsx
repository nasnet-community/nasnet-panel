import { component$ } from "@builder.io/qwik";

export const HTTPProxyServerEasy = component$(() => {
  // In easy mode, HTTP Proxy is not shown as it's not one of the default protocols
  // This component is just a placeholder
  return null;
});