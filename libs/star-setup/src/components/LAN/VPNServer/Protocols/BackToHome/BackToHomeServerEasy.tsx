import { component$ } from "@builder.io/qwik";

export const BackToHomeServerEasy = component$(() => {
  // In easy mode, BackToHome is not shown as it's not one of the default protocols
  // This component is just a placeholder
  return null;
});