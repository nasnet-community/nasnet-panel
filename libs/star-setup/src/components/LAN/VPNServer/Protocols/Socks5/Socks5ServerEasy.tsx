import { component$ } from "@builder.io/qwik";

export const Socks5ServerEasy = component$(() => {
  // In easy mode, Socks5 is not shown as it's not one of the default protocols
  // This component is just a placeholder
  return null;
});