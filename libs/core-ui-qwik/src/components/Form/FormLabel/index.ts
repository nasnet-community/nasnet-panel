/**
 * FormLabel Component
 *
 * The FormLabel provides a consistent and accessible way to label form fields.
 * Now with enhanced responsive design, fluid typography, and mobile optimization.
 */

import { FormLabel } from "./FormLabel";
import type { FormLabelProps, FormLabelSize, FormLabelFluidSize } from "./FormLabel";

export {
  FormLabel,

  // Types
  type FormLabelProps,
  type FormLabelSize,
  type FormLabelFluidSize,
};
