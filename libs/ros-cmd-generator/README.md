# ros-cmd-generator

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build ros-cmd-generator` to build the library.

## Running unit tests

Run `nx test ros-cmd-generator` to execute the unit tests via [Vitest](https://vitest.dev/).
