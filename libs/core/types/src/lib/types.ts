/**
 * Get library identifier
 *
 * @returns Library name identifier
 * @example
 * ```typescript
 * const name = types(); // Returns 'types'
 * ```
 */
export function types(): string {
  return 'types';
}
