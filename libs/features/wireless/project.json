{
  "name": "@nasnet/features/wireless",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/features/wireless/src",
  "projectType": "library",
  "tags": ["type:lib", "scope:features"],
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "lintFilePatterns": ["libs/features/wireless/**/*.{ts,tsx,js,jsx}"]
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "tsc --noEmit --project libs/features/wireless/tsconfig.lib.json",
          "tsc --noEmit --project libs/features/wireless/tsconfig.spec.json",
          "tsc --noEmit --project libs/features/wireless/tsconfig.storybook.json"
        ],
        "parallel": true
      }
    }
  }
}
