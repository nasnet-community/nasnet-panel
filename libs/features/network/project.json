{
  "name": "@nasnet/features/network",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/features/network/src",
  "projectType": "library",
  "tags": ["type:lib", "scope:features"],
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "lintFilePatterns": ["libs/features/network/**/*.{ts,tsx,js,jsx}"]
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "tsc --noEmit --project libs/features/network/tsconfig.lib.json",
          "tsc --noEmit --project libs/features/network/tsconfig.spec.json",
          "tsc --noEmit --project libs/features/network/tsconfig.storybook.json"
        ],
        "parallel": true
      }
    }
  }
}
