{
  "name": "@nasnet/features/diagnostics",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/features/diagnostics/src",
  "projectType": "library",
  "tags": ["type:lib", "scope:features"],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit --project libs/features/diagnostics/tsconfig.json"
      }
    }
  }
}
