{
  "name": "@nasnet/features/diagnostics",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/features/diagnostics/src",
  "projectType": "library",
  "tags": ["type:lib", "scope:features"],
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "lintFilePatterns": ["libs/features/diagnostics/**/*.{ts,tsx,js,jsx}"]
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "tsc --noEmit --project libs/features/diagnostics/tsconfig.lib.json",
          "tsc --noEmit --project libs/features/diagnostics/tsconfig.spec.json",
          "tsc --noEmit --project libs/features/diagnostics/tsconfig.storybook.json"
        ],
        "parallel": true
      }
    }
  }
}
