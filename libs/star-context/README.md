# star-context

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build star-context` to build the library.

## Running unit tests

Run `nx test star-context` to execute the unit tests via [Vitest](https://vitest.dev/).
